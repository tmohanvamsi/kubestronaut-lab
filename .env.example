# Copy this file to .env.local and fill in real values
# .env.local is gitignored — never commit it

# Grafana admin password (used in monitoring/prometheus/values.yaml)
GRAFANA_ADMIN_PASSWORD=change-me-before-use
